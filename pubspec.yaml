name: unir_pdf
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.3
  desktop_drop: ^0.4.4
  syncfusion_flutter_pdf: ^26.2.13
  path_provider: ^2.1.4
  path: ^1.9.0
  cross_file: ^0.3.5+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
